import './sass/App.scss';

const App = () => {
  return (
    <div className="App">
      <h1>Cinephile</h1>
      {/* 
      Pages
      - Rules (How to Play)
        - Filmography (like Categories)
        - Head On (like Heads up)
        - Movie, Actor (kind of like Telestrations bouncing between movies and actors)
        - Take, Six (kind of like uno in that you are trying to get rid of your cards but have to do so by connecting them to the card played) this is had
        - Six Degrees (connecting the two cards in six degrees or less)
      - Game itself
        - just flipping cards and giving option to flip based on game mode and discard/skip if too hard.
        - for a time limit you can add a timer if you wish. 
      - Deck (this will change based on tmdb popular movies/actors);
        - movies 
        - actors 
      - trending 
      https://api.themoviedb.org/3/trending/person/week?api_key={api_key}
        - web scrape and add the remixes:
        https://www.cinephilegame.com/remixes
      */}
    </div>
  );
}

export default App;